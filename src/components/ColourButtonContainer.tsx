import styled from "styled-components";

const ColourButtonContainer = styled('div')`
    margin-top: 10px;
    display: flex;
    justify-content: space-around;
    flex-wrap: wrap;
`

export default ColourButtonContainer